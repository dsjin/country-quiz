// import type { QuizItem } from '../types/quiz'

// const getQuizList = async (): Promise<QuizItem[]> => {
//   try {
//     const res = await fetch('')
//     if (!res.ok) {
//       throw new Error(`Response status: ${res.status}`);
//     }
//     const json = await res.json()
//   } catch (err) {
//     console.error(err)
//   }
// }

